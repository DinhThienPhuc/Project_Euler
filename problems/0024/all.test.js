const solution = require("./solution.js")
const answer = require("./answer.js")
describe("[LEXICOGRAPHIC_PERMUTATIONS] - Test solution:", () => {
test("Validate invalid input", async () => { expect(solution()).toEqual() })
})
describe("[LEXICOGRAPHIC_PERMUTATIONS] - Test answer:", () => {
test("No answer recommended", async () => { expect(answer()).toEqual() })
})
